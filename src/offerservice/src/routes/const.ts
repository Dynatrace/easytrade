export enum OFFERS_ENDPOINTS {
    GET_OFFERS = "/api/offers/:platform",
}

export enum SIGNUP_ENDPOINTS {
    NEW_USER = "/api/signup",
}

export enum VERSION_ENDPOINTS {
    GET_VERSION = "/api/version",
}
