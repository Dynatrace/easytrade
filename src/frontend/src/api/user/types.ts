export type User = {
    id: string
    firstName: string
    lastName: string
    packageType: string
    email: string
    address: string
}

export type Balance = {
    accountId: string
    value: number
}

export type PresetUser = {
    id: string
    firstName: string
    lastName: string
}
