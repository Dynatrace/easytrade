name: Build and push easytrade
on:
  push:
    branches:
      - "main"
    paths:
      - .github/**
      - kubernetes-manifests/**
      - skaffold.yaml
      - src/**
      - helm/**
  workflow_dispatch:

  workflow_call:
    secrets:
      GAR_CREDENTIALS:
        required: true
      KUBECONFIG:
        required: true
      OQR_CLIENT_ID:
        required: true
      OQR_CLIENT_SECRET:
        required: true
      OQR_URL:
        required: true
      SNYK_API_TOKEN:
        required: true
      SSH_PRIVATE_KEY:
        required: true
      SSO_URL:
        required: true

env:
  GAR_ADDRESS: europe-docker.pkg.dev
  NAMESPACE: easytrade
  CONTAINER_REGISTRY: europe-docker.pkg.dev/dynatrace-demoability/docker/easytrade
  HELM_REGISTRY: oci://europe-docker.pkg.dev/dynatrace-demoability/helm
  HELM_CHART_PATH: helm/easytrade

jobs:
  snyk:
    uses: ./.github/workflows/reusable-snyk.yaml
    permissions:
      security-events: write
      contents: read
      actions: read
    secrets:
      SNYK_API_TOKEN: ${{ secrets.SNYK_API_TOKEN }}

  calc-version:
    uses: ./.github/workflows/reusable-calc-version.yaml

  list-services:
    runs-on: ubuntu-24.04
    outputs:
      services: ${{ steps.list.outputs.services }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: List buildable services
        id: list
        uses: ./.github/actions/list-build-services

  build-push-image:
    if: needs.calc-version.outputs.already-released == 'false'
    needs:
      - calc-version
      - list-services
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        service: ${{ fromJson(needs.list-services.outputs.services) }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Login to Google Artifact Registry
        run: |
          echo "${{ secrets.GAR_CREDENTIALS }}" | \
            docker login -u _json_key_base64 --password-stdin "${{ env.GAR_ADDRESS }}"

      - name: Set application version
        uses: ./.github/actions/set-version
        with:
          version: ${{ needs.calc-version.outputs.version }}

      - name: Build and push ${{ matrix.service }} to docker repository
        uses: ./.github/actions/build-push-images
        with:
          push: true
          push-latest: ${{ needs.calc-version.outputs.is-release }}
          tag: ${{ needs.calc-version.outputs.version }}
          registry: ${{ env.CONTAINER_REGISTRY }}
          service: ${{ matrix.service }}

  build-push-helm:
    if: needs.calc-version.outputs.already-released == 'false'
    runs-on: ubuntu-24.04
    needs: calc-version
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Build and push Helm chart
        uses: ./.github/actions/build-push-helm
        with:
          chart-path: ${{ env.HELM_CHART_PATH }}
          push: true
          registry-url: oci://europe-docker.pkg.dev/dynatrace-demoability/helm
          version: ${{ needs.calc-version.outputs.version }}
          credentials: ${{ secrets.GAR_CREDENTIALS }}

  build-artifact:
    if: needs.calc-version.outputs.already-released == 'false'
    runs-on: ubuntu-24.04
    needs:
      - calc-version
      - build-push-image
    outputs:
      artifact-dir: ${{ steps.artifacts.outputs.artifact-dir }}
      artifact-name: ${{ steps.artifacts.outputs.artifact-name }}
    env:
      REGISTRY: europe-docker.pkg.dev/dynatrace-demoability/docker/easytrade
      TAG: ${{ needs.calc-version.outputs.version }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Prepare build artifacts
        id: artifacts
        run: |
          ARTIFACT_DIR=./build-artifacts/
          mkdir -p $ARTIFACT_DIR
          envsubst < compose.yaml > $ARTIFACT_DIR/compose.yaml
          echo "artifact-dir=$ARTIFACT_DIR" >> $GITHUB_OUTPUT
          echo "artifact-name=docker-compose" >> $GITHUB_OUTPUT

      - name: Upload build artifacts
        uses: actions/upload-artifact@v6
        with:
          name: docker-compose
          path: ${{ steps.artifacts.outputs.artifact-dir }}
          if-no-files-found: error

  promote:
    if: needs.calc-version.outputs.is-release == 'true'
    needs:
      - calc-version
      - build-push-image
      - build-push-helm
      - build-artifact
    permissions:
      contents: write
      actions: write
    uses: ./.github/workflows/reusable-promote-release.yaml
    with:
      version: ${{ needs.calc-version.outputs.version }}
      trigger-deploy: false
      artifact-name: ${{ needs.build-artifact.outputs.artifact-name }}
      artifact-dir: ${{ needs.build-artifact.outputs.artifact-dir }}
