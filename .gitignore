.vscode/*
!.vscode/mcp.json

### Go binaries ###
src/feature-flag-service/feature-flag-service

### proto generation tooling (downloaded, not checked in) ###
src/proto/.tools/

### helm dependency artifacts (regenerated by `make helm-deps`) ###
helm/easytrade/Chart.lock
helm/easytrade/charts/*.tgz

### Python bytecode cache (regenerated on every run of scripts/*.py) ###
__pycache__/
*.pyc
